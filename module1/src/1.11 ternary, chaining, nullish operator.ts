{
  // ternary oparator
  // chaining operator
  // nullish operator

  // Learning ternary operator

  const age: number = 14;

  const isAge: string = age >= 18 ? "Adult" : "Not adult";
  console.log(isAge);
  console.log(isAge);
}
